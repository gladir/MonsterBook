Program MakeColor;

Uses Dialex,Isatex,Systex,Systems;

Var
 Colors:Array[0..8]of MatrixKrs;

Procedure InitColorsDefault;Begin
 With Colors[MtxMonochrome]do Begin
  LastBar.Normal            := (Black        shl 4) + White;
  LastBar.Higher            := (Black        shl 4) + LightGray;
  Menu.Normal               := (White        shl 4) + Black;
  Menu.High                 := (White        shl 4) + LightGray;
  Menu.Select               := (Black        shl 4) + White;
  Desktop.Tapiserie         := (Black        shl 4) + LightGray;
  Desktop.DialStatus        := (LightGray    shl 4) + Black;
  Dialog.Window.Title       := (White        shl 4) + Black;
  Dialog.Window.Border      := (Black        shl 4) + White;
  Dialog.Window.Icon        := (Black        shl 4) + White;
  Dialog.Window.Sel         := (White        shl 4) + Black;
  Dialog.Window.High        := (Black        shl 4) + LightGray;
  Dialog.Window.Key         := (White        shl 4) + Black;
  Dialog.Window.kShade      := (Black        shl 4) + Black;
  Dialog.Window.kSel        := (White        shl 4) + LightRed;
  Dialog.Env.BarSelInactive := (Blue         shl 4) + White;
  Dialog.Env.BarSelect      := (DarkGray     shl 4) + White;
  Dialog.Env.List.Title         := (White        shl 4) + Black;
  Dialog.Env.List.Border        := (Black        shl 4) + White;
  Dialog.Env.List.Icon          := (Black        shl 4) + White;
  Dialog.Env.List.Sel           := (White        shl 4) + Black;
  Dialog.Env.List.High          := (Black        shl 4) + LightCyan;
  Dialog.Env.List.Key           := (White        shl 4) + Black;
  Dialog.Env.List.kShade        := (Black        shl 4) + Black;
  Dialog.Env.List.kSel          := (White        shl 4) + LightRed;
  ErrorWin.Window.Border      := (LightRed     shl 4) + White;
  ErrorWin.Window.Title       := (White        shl 4) + LightBlue;
  ErrorWin.Window.Msg         := (LightRed     shl 4) + Yellow;
  ErrorWin.Window.KeyActif    := (White        shl 4) + LightRed;
  ErrorWin.Window.KeyUnactif  := (White        shl 4) + Black;
  ErrorWin.Window.KeyShade    := (LightRed     shl 4) + DarkGray;
  ErrorWin.Env.Input        := (LightRed     shl 4) + Red;
  WarningWin.Border         := (Yellow       shl 4) + White;
  WarningWin.Title          := (White        shl 4) + LightBlue;
  WarningWin.Msg            := (Yellow       shl 4) + Black;
  WarningWin.KeyActif       := (White        shl 4) + LightRed;
  WarningWin.KeyUnactif     := (White        shl 4) + Black;
  WarningWin.KeyShade       := (Yellow       shl 4) + DarkGray;
  RemWin.Border             := (LightBlue    shl 4) + White;
  RemWin.Title              := (White        shl 4) + Black;
  RemWin.Msg                := (LightBlue    shl 4) + LightCyan;
  RemWin.KeyActif           := (White        shl 4) + LightRed;
  RemWin.KeyUnactif         := (White        shl 4) + Black;
  RemWin.KeyShade           := (LightBlue    shl 4) + Black;
  OpenWin.Window.Title        := (White        shl 4) + Black;
  OpenWin.Window.Border       := (LightBlue    shl 4) + White;
  OpenWin.Window.Icon         := (Black        shl 4) + White;
  OpenWin.Window.Sel          := (Blue         shl 4) + White;
  OpenWin.Window.High         := (LightBlue    shl 4) + LightCyan;
  OpenWin.Window.Key          := (White        shl 4) + Black;
  OpenWin.Window.kShade       := (LightBlue    shl 4) + Black;
  OpenWin.Window.kSel         := (White        shl 4) + LightRed;
  OpenWin.Env.Input         := (Blue         shl 4) + White;
  OpenWin.Env.Dir           := (Blue         shl 4) + Yellow;
  MalteDos.Window.Title       := (White        shl 4) + Black;
  MalteDos.Window.Border      := (LightBlue    shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Blue         shl 4) + White;
  MalteDos.Window.High        := (LightBlue    shl 4) + LightCyan;
  MalteDos.Window.Key         := (White        shl 4) + Black;
  MalteDos.Window.kShade      := (LightBlue    shl 4) + Black;
  MalteDos.Window.kSel        := (White        shl 4) + LightRed;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Icon          := (Black        shl 4) + White;
  Editor.Window.Border        := (Black        shl 4) + White;
  Editor.Window.Title         := (White        shl 4) + Black;
  Editor.Window.Sel           := (Black        shl 4) + White;
  Editor.Window.High          := (LightBlue    shl 4) + LightCyan;
  Editor.Window.kSel          := (White        shl 4) + LightRed;
  Editor.Window.Key           := (White        shl 4) + Black;
  Editor.Window.kShade        := (LightBlue    shl 4) + Black;
  Editor.Env.BarInfo        := (White        shl 4) + Black;
  Editor.Env.Default        := (Black        shl 4) + LightMagenta;
  Editor.Env.Typewriter     := (Black        shl 4) + LightGray;
  Editor.Env.Rem            := (Black        shl 4) + Yellow;
  Editor.Env.ResWord        := (Black        shl 4) + White;
  Editor.Env.Chars          := (Black        shl 4) + LightRed;
  Editor.Env.Symbol         := (Black        shl 4) + LightCyan;
  Editor.Env.AsmInst        := (Black        shl 4) + LightGreen;
  Editor.Env.Number         := (Black        shl 4) + LightCyan;
  Editor.Env.Z              := (LightMagenta shl 4) + Black;
  Editor.Env.Normal         := (Black        shl 4) + White;
  Editor.Env.Pos            := (White        shl 4) + Blue;
  Editor.Env.Insert         := (White        shl 4) + LightMagenta;
  Editor.Env.Modified       := (White        shl 4) + LightRed;
  Editor.OutZone            := (LightGray    shl 4) + Black;
  PersonalJournal.Window.Title  := (LightMagenta shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (LightMagenta shl 4) + White;
  PersonalJournal.Window.High   := (Blue         shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (White        shl 4) + Black;
  PersonalJournal.Window.kShade := (Blue         shl 4) + Black;
  PersonalJournal.Window.kSel   := (White        shl 4) + LightRed;
  PersonalJournal.Env.BarInfo := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Default := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos     := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Insert  := (LightMagenta shl 4) + Magenta;
  ViewAscii.Window.Border       := (LightBlue    shl 4) + White;
  ViewAscii.Window.Title        := (White        shl 4) + Black;
  ViewAscii.Window.Sel          := (Blue         shl 4) + White;
  ViewAscii.Window.High         := (LightBlue    shl 4) + LightCyan;
  ViewAscii.Window.kSel         := (White        shl 4) + LightRed;
  ViewAscii.Window.Key          := (White        shl 4) + Black;
  ViewAscii.Window.kShade       := (LightBlue    shl 4) + Black;
  ViewAscii.Env.BarInfo       := (LightCyan    shl 4) + Black;
  ViewAscii.Env.Default       := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Default       := (Blue         shl 4) + LightGray;
  ViewAscii.Env.Rem           := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord       := (Blue         shl 4) + White;
  ViewAscii.Env.Chars         := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol        := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst       := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number        := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z             := (LightMagenta shl 4) + Blue;
  ViewAscii.Env.Normal        := (Blue         shl 4) + White;
  ViewAscii.Env.Pos           := (LightCyan    shl 4) + Blue;
  ViewAscii.Env.Insert        := (LightCyan    shl 4) + LightMagenta;
  ViewAscii.Env.Modified      := (LightCyan    shl 4) + LightRed;
  HexView.Window.Title  := (White        shl 4) + Black;
  HexView.Window.Border := (LightBlue    shl 4) + White;
  HexView.Window.Icon   := (Black        shl 4) + White;
  HexView.Window.Sel    := (Blue         shl 4) + White;
  HexView.Window.High   := (LightBlue    shl 4) + LightCyan;
  HexView.Window.Key    := (White        shl 4) + Black;
  HexView.Window.kShade := (LightBlue    shl 4) + Black;
  HexView.Window.kSel   := (White        shl 4) + LightRed;
  HexView.Env.Input   := (Blue         shl 4) + White;
  EditButt.Title  := (White        shl 4) + Black;
  EditButt.Border := (LightBlue    shl 4) + White;
  EditButt.Icon   := (Black        shl 4) + White;
  EditButt.Sel    := (Blue         shl 4) + White;
  EditButt.High   := (LightBlue    shl 4) + LightCyan;
  EditButt.Key    := (White        shl 4) + Black;
  EditButt.kShade := (LightBlue    shl 4) + Black;
  EditButt.kSel   := (White        shl 4) + LightRed;
  Dial.Window.Title      := (White        shl 4) + Black;
  Dial.Window.Border     := (Black        shl 4) + White;
  Dial.Window.Icon       := (Black        shl 4) + White;
  Dial.Window.Sel        := (Blue         shl 4) + White;
  Dial.Window.High       := (LightBlue    shl 4) + LightCyan;
  Dial.Window.Key        := (White        shl 4) + Black;
  Dial.Window.kShade     := (Black        shl 4) + Black;
  Dial.Window.kSel       := (White        shl 4) + LightRed;
  Dial.Env.BarUnselect := (LightBlue    shl 4) + White;
  Dial.Env.BarSelect   := (Blue         shl 4) + White;
  Draw.Window.Title      := (White        shl 4) + Black;
  Draw.Window.Border     := (LightBlue    shl 4) + White;
  Draw.Window.Icon       := (Black        shl 4) + White;
  Draw.Window.Sel        := (Blue         shl 4) + White;
  Draw.Window.High       := (LightBlue    shl 4) + LightCyan;
  Draw.Window.Key        := (White        shl 4) + Black;
  Draw.Window.kShade     := (LightBlue    shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (White        shl 4) + Black;
  FileManager.Window.Border:= (Black        shl 4) + White;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (Blue         shl 4) + White;
  FileManager.Window.High  := (LightBlue    shl 4) + LightCyan;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (LightBlue    shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.SubTitle   := (White        shl 4) + Black;
 End;
 With Colors[MtxBlackWhite]do Begin
  LastBar.Normal            := (Black        shl 4) + White;
  LastBar.Higher            := (Black        shl 4) + LightGray;
  Menu.Normal               := (White        shl 4) + Black;
  Menu.High                 := (White        shl 4) + LightGray;
  Menu.Select               := (Black        shl 4) + White;
  Desktop.Tapiserie         := (Black        shl 4) + LightGray;
  Desktop.DialStatus        := (LightGray    shl 4) + Black;
  Dialog.Window.Title         := (White        shl 4) + Black;
  Dialog.Window.Border        := (Black        shl 4) + White;
  Dialog.Window.Icon          := (Black        shl 4) + White;
  Dialog.Window.Sel           := (White        shl 4) + Black;
  Dialog.Window.High          := (Black        shl 4) + LightGray;
  Dialog.Window.Key           := (White        shl 4) + Black;
  Dialog.Window.kShade        := (Black        shl 4) + Black;
  Dialog.Window.kSel          := (White        shl 4) + LightGray;
  Dialog.Env.BarSelInactive := (Black        shl 4) + White;
  Dialog.Env.BarSelect      := (DarkGray     shl 4) + White;
  Dialog.Env.List.Title         := (White        shl 4) + Black;
  Dialog.Env.List.Border        := (LightGray    shl 4) + White;
  Dialog.Env.List.Icon          := (Black        shl 4) + White;
  Dialog.Env.List.Sel           := (White        shl 4) + Black;
  Dialog.Env.List.High          := (Black        shl 4) + LightGray;
  Dialog.Env.List.Key           := (White        shl 4) + Black;
  Dialog.Env.List.kShade        := (LightGray    shl 4) + Black;
  Dialog.Env.List.kSel          := (White        shl 4) + LightGray;
  ErrorWin.Window.Border      := (Black        shl 4) + White;
  ErrorWin.Window.Title       := (White        shl 4) + Black;
  ErrorWin.Window.Msg         := (Black        shl 4) + White;
  ErrorWin.Window.KeyActif    := (White        shl 4) + LightGray;
  ErrorWin.Window.KeyUnactif  := (White        shl 4) + Black;
  ErrorWin.Window.KeyShade    := (Black        shl 4) + DarkGray;
  ErrorWin.Env.Input        := (LightGray    shl 4) + Red;
  WarningWin.Border         := (Black        shl 4) + White;
  WarningWin.Title          := (White        shl 4) + Black;
  WarningWin.Msg            := (Black        shl 4) + White;
  WarningWin.KeyActif       := (White        shl 4) + LightRed;
  WarningWin.KeyUnactif     := (White        shl 4) + Black;
  WarningWin.KeyShade       := (Black        shl 4) + DarkGray;
  RemWin.Border             := (Black        shl 4) + White;
  RemWin.Title              := (White        shl 4) + Black;
  RemWin.Msg                := (Black        shl 4) + LightCyan;
  RemWin.KeyActif           := (White        shl 4) + LightRed;
  RemWin.KeyUnactif         := (White        shl 4) + Black;
  RemWin.KeyShade           := (Black        shl 4) + Black;
  OpenWin.Window.Title        := (White        shl 4) + Black;
  OpenWin.Window.Border       := (Black        shl 4) + White;
  OpenWin.Window.Icon         := (Black        shl 4) + White;
  OpenWin.Window.Sel          := (Blue         shl 4) + White;
  OpenWin.Window.High         := (Black        shl 4) + LightCyan;
  OpenWin.Window.Key          := (White        shl 4) + Black;
  OpenWin.Window.kShade       := (Black        shl 4) + Black;
  OpenWin.Window.kSel         := (White        shl 4) + LightRed;
  OpenWin.Env.Input         := (Black        shl 4) + White;
  OpenWin.Env.Dir           := (Black        shl 4) + Yellow;
  MalteDos.Window.Title       := (White        shl 4) + Black;
  MalteDos.Window.Border      := (LightBlue    shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Blue         shl 4) + White;
  MalteDos.Window.High        := (LightBlue    shl 4) + LightCyan;
  MalteDos.Window.Key         := (White        shl 4) + Black;
  MalteDos.Window.kShade      := (LightBlue    shl 4) + Black;
  MalteDos.Window.kSel        := (White        shl 4) + LightRed;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Border        := (Black        shl 4) + White;
  Editor.Window.Title         := (White        shl 4) + Black;
  Editor.Window.Sel           := (Black        shl 4) + White;
  Editor.Window.Icon          := (Black        shl 4) + White;
  Editor.Window.High          := (LightGray    shl 4) + LightCyan;
  Editor.Window.kSel          := (White        shl 4) + LightRed;
  Editor.Window.Key           := (White        shl 4) + Black;
  Editor.Window.kShade        := (Black        shl 4) + Black;
  Editor.Env.BarInfo        := (White        shl 4) + Black;
  Editor.Env.Default        := (Black        shl 4) + LightMagenta;
  Editor.Env.Typewriter     := (Black        shl 4) + LightGray;
  Editor.Env.Rem            := (Black        shl 4) + Yellow;
  Editor.Env.ResWord        := (Black        shl 4) + White;
  Editor.Env.Chars          := (Black        shl 4) + LightRed;
  Editor.Env.Symbol         := (Black        shl 4) + LightCyan;
  Editor.Env.AsmInst        := (Black        shl 4) + LightGreen;
  Editor.Env.Number         := (Black        shl 4) + LightBlue;
  Editor.Env.Z              := (LightGray    shl 4) + Black;
  Editor.Env.Normal         := (Black        shl 4) + White;
  Editor.Env.Pos            := (White        shl 4) + Black;
  Editor.Env.Insert         := (White        shl 4) + LightMagenta;
  Editor.Env.Modified       := (White        shl 4) + LightRed;
  Editor.OutZone            := (LightGray    shl 4) + Black;
  PersonalJournal.Window.Title  := (LightMagenta shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (LightMagenta shl 4) + White;
  PersonalJournal.Window.High   := (Blue         shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (White        shl 4) + Black;
  PersonalJournal.Window.kShade := (Blue         shl 4) + Black;
  PersonalJournal.Window.kSel   := (White        shl 4) + LightRed;
  PersonalJournal.Env.BarInfo := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Default := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos     := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Insert  := (LightMagenta shl 4) + Magenta;
  ViewAscii.Window.Border       := (LightBlue    shl 4) + White;
  ViewAscii.Window.Title        := (White        shl 4) + Black;
  ViewAscii.Window.Sel          := (Blue         shl 4) + White;
  ViewAscii.Window.High         := (LightBlue    shl 4) + LightCyan;
  ViewAscii.Window.kSel         := (White        shl 4) + LightRed;
  ViewAscii.Window.Key          := (White        shl 4) + Black;
  ViewAscii.Window.kShade       := (LightBlue    shl 4) + Black;
  ViewAscii.Env.BarInfo       := (LightCyan    shl 4) + Black;
  ViewAscii.Env.Default       := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Rem           := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord       := (Blue         shl 4) + White;
  ViewAscii.Env.Chars         := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol        := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst       := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number        := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z             := (LightMagenta shl 4) + Blue;
  ViewAscii.Env.Normal        := (Blue         shl 4) + White;
  ViewAscii.Env.Pos           := (LightCyan    shl 4) + Blue;
  ViewAscii.Env.Insert        := (LightCyan    shl 4) + LightMagenta;
  ViewAscii.Env.Modified      := (LightCyan    shl 4) + LightRed;
  HexView.Window.Title        := (White        shl 4) + Black;
  HexView.Window.Border       := (LightBlue    shl 4) + White;
  HexView.Window.Icon         := (Black        shl 4) + White;
  HexView.Window.Sel          := (Blue         shl 4) + White;
  HexView.Window.High         := (LightBlue    shl 4) + LightCyan;
  HexView.Window.Key          := (White        shl 4) + Black;
  HexView.Window.kShade       := (LightBlue    shl 4) + Black;
  HexView.Window.kSel         := (White        shl 4) + LightRed;
  HexView.Env.Input         := (Blue         shl 4) + White;
  EditButt.Title       := (White        shl 4) + Black;
  EditButt.Border      := (LightBlue    shl 4) + White;
  EditButt.Icon        := (Black        shl 4) + White;
  EditButt.Sel         := (Blue         shl 4) + White;
  EditButt.High        := (LightBlue    shl 4) + LightCyan;
  EditButt.Key         := (White        shl 4) + Black;
  EditButt.kShade      := (LightBlue    shl 4) + Black;
  EditButt.kSel        := (White        shl 4) + LightRed;
  Dial.Window.Title         := (White        shl 4) + Black;
  Dial.Window.Border        := (LightBlue    shl 4) + White;
  Dial.Window.Icon          := (Black        shl 4) + White;
  Dial.Window.Sel           := (Blue         shl 4) + White;
  Dial.Window.High          := (LightBlue    shl 4) + LightCyan;
  Dial.Window.Key           := (White        shl 4) + Black;
  Dial.Window.kShade        := (LightBlue    shl 4) + Black;
  Dial.Window.kSel          := (White        shl 4) + LightRed;
  Dial.Env.BarUnselect    := (LightBlue    shl 4) + White;
  Dial.Env.BarSelect      := (Blue         shl 4) + White;
  Draw.Window.Title         := (White        shl 4) + Black;
  Draw.Window.Border        := (LightBlue    shl 4) + White;
  Draw.Window.Icon          := (Black        shl 4) + White;
  Draw.Window.Sel           := (Blue         shl 4) + White;
  Draw.Window.High          := (LightBlue    shl 4) + LightCyan;
  Draw.Window.Key           := (White        shl 4) + Black;
  Draw.Window.kShade        := (LightBlue    shl 4) + Black;
  Draw.Window.kSel          := (White        shl 4) + LightRed;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (White        shl 4) + Black;
  FileManager.Window.Border:= (Black        shl 4) + White;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (White        shl 4) + Black;
  FileManager.Window.High  := (Black        shl 4) + White;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (Black        shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + Black;
  FileManager.SubTitle   := (White        shl 4) + Black;
 End;
 With Colors[MtxBlueCGA]do Begin
  LastBar.Normal     := (Blue         shl 4) + White;
  LastBar.Higher     := (Blue         shl 4) + Yellow;
  Menu.Normal        := (Cyan         shl 4) + White;
  Menu.High          := (Cyan         shl 4) + LightCyan;
  Menu.Select        := (Magenta      shl 4) + Yellow;
  Desktop.Tapiserie         := (Blue         shl 4) + LightBlue;
  Desktop.DialStatus        := (LightGray    shl 4) + Black;
  Dialog.Window.Title  := (Green        shl 4) + Yellow;
  Dialog.Window.Border := (Green        shl 4) + White;
  Dialog.Window.Icon   := (Green        shl 4) + LightGreen;
  Dialog.Window.Sel    := (Magenta      shl 4) + Yellow;
  Dialog.Window.High   := (Green        shl 4) + LightCyan;
  Dialog.Window.Key    := (Cyan         shl 4) + White;
  Dialog.Window.kShade := (Green        shl 4) + Black;
  Dialog.Window.kSel   := (Cyan         shl 4) + Yellow;
  Dialog.Env.BarSelInactive := (Magenta      shl 4) + White;
  Dialog.Env.BarSelect      := (Magenta      shl 4) + Yellow;
  Dialog.Env.List.Title  := (Green        shl 4) + Yellow;
  Dialog.Env.List.Border := (Green        shl 4) + White;
  Dialog.Env.List.Icon   := (Green        shl 4) + LightGreen;
  Dialog.Env.List.Sel    := (Magenta      shl 4) + Yellow;
  Dialog.Env.List.High   := (Green        shl 4) + LightCyan;
  Dialog.Env.List.Key    := (Cyan         shl 4) + White;
  Dialog.Env.List.kShade := (Green        shl 4) + Black;
  Dialog.Env.List.kSel   := (Cyan         shl 4) + Yellow;
  ErrorWin.Window.Border      := (Red          shl 4) + White;
  ErrorWin.Window.Title       := (Red          shl 4) + Yellow;
  ErrorWin.Window.Msg         := (Red          shl 4) + Yellow;
  ErrorWin.Window.KeyActif    := (Cyan         shl 4) + Yellow;
  ErrorWin.Window.KeyUnactif  := (Cyan         shl 4) + White;
  ErrorWin.Window.KeyShade    := (Red          shl 4) + Black;
  ErrorWin.Env.Input        := (Blue         shl 4) + LightCyan;
  WarningWin.Border         := (Brown        shl 4) + White;
  WarningWin.Title          := (Brown        shl 4) + Yellow;
  WarningWin.Msg            := (Brown        shl 4) + Yellow;
  WarningWin.KeyActif       := (Cyan         shl 4) + Yellow;
  WarningWin.KeyUnactif     := (Cyan         shl 4) + White;
  WarningWin.KeyShade       := (Brown        shl 4) + Black;
  RemWin.Border             := (Green        shl 4) + White;
  RemWin.Title              := (Green        shl 4) + Yellow;
  RemWin.Msg                := (Green        shl 4) + LightCyan;
  RemWin.KeyActif           := (Cyan         shl 4) + Yellow;
  RemWin.KeyUnactif         := (Cyan         shl 4) + White;
  RemWin.KeyShade           := (Green        shl 4) + Black;
  OpenWin.Window.Title        := (Green        shl 4) + Yellow;
  OpenWin.Window.Border       := (Green        shl 4) + White;
  OpenWin.Window.Icon         := (Green        shl 4) + LightGreen;
  OpenWin.Window.Sel          := (Magenta      shl 4) + Yellow;
  OpenWin.Window.High         := (Green        shl 4) + LightCyan;
  OpenWin.Window.Key          := (Cyan         shl 4) + White;
  OpenWin.Window.kShade       := (Green        shl 4) + Black;
  OpenWin.Window.kSel         := (Cyan         shl 4) + Yellow;
  OpenWin.Env.Input         := (Blue         shl 4) + White;
  OpenWin.Env.Dir           := (Blue         shl 4) + Yellow;
  MalteDos.Window.Title       := (Black        shl 4) + Yellow;
  MalteDos.Window.Border      := (Black        shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Black        shl 4) + LightCyan;
  MalteDos.Window.High        := (Black        shl 4) + LightBlue;
  MalteDos.Window.Key         := (Cyan         shl 4) + White;
  MalteDos.Window.kShade      := (Black        shl 4) + Black;
  MalteDos.Window.kSel        := (Cyan         shl 4) + Yellow;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Border        := (Blue         shl 4) + White;
  Editor.Window.Title         := (Blue         shl 4) + Yellow;
  Editor.Window.Sel           := (Magenta      shl 4) + Yellow;
  Editor.Window.High          := (Blue         shl 4) + LightCyan;
  Editor.Window.kSel          := (Cyan         shl 4) + Yellow;
  Editor.Window.Key           := (Cyan         shl 4) + White;
  Editor.Window.kShade        := (Blue         shl 4) + Black;
  Editor.Env.BarInfo        := (LightCyan    shl 4) + Black;
  Editor.Env.Default        := (Blue         shl 4) + LightMagenta;
  Editor.Env.Typewriter     := (Blue         shl 4) + LightGray;
  Editor.Env.Rem            := (Blue         shl 4) + Yellow;
  Editor.Env.ResWord        := (Blue         shl 4) + White;
  Editor.Env.Chars          := (Blue         shl 4) + LightRed;
  Editor.Env.Symbol         := (Blue         shl 4) + LightCyan;
  Editor.Env.AsmInst        := (Blue         shl 4) + LightGreen;
  Editor.Env.Number         := (Blue         shl 4) + LightBlue;
  Editor.Env.Z              := (LightMagenta shl 4) + Blue;
  Editor.Env.Normal         := (Blue         shl 4) + White;
  Editor.Env.Pos            := (Cyan         shl 4) + Blue;
  Editor.Env.Insert         := (Cyan         shl 4) + LightMagenta;
  Editor.Env.Modified       := (Cyan         shl 4) + LightRed;
  Editor.OutZone            := (LightGray    shl 4) + Black;
  PersonalJournal.Window.Title  := (Magenta      shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (Blue         shl 4) + White;
  PersonalJournal.Window.High   := (Magenta      shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (Cyan         shl 4) + White;
  PersonalJournal.Window.kShade := (Magenta      shl 4) + Black;
  PersonalJournal.Window.kSel   := (Cyan         shl 4) + Yellow;
  PersonalJournal.Env.BarInfo    := (Red          shl 4) + Magenta;
  PersonalJournal.Env.Default    := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos        := (Red          shl 4) + Magenta;
  PersonalJournal.Env.Insert     := (Red          shl 4) + Magenta;
  ViewAscii.Window.Border          := (Blue         shl 4) + White;
  ViewAscii.Window.Title           := (Blue         shl 4) + Yellow;
  ViewAscii.Window.Sel             := (Magenta      shl 4) + White;
  ViewAscii.Window.High            := (Blue         shl 4) + LightCyan;
  ViewAscii.Window.kSel            := (Cyan         shl 4) + Yellow;
  ViewAscii.Window.Key             := (Cyan         shl 4) + White;
  ViewAscii.Window.kShade          := (Blue         shl 4) + Black;
  ViewAscii.Env.BarInfo          := (Cyan         shl 4) + Black;
  ViewAscii.Env.Default          := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Rem              := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord          := (Blue         shl 4) + White;
  ViewAscii.Env.Chars            := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol           := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst          := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number           := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z                := (Magenta      shl 4) + Blue;
  ViewAscii.Env.Normal           := (Blue         shl 4) + White;
  ViewAscii.Env.Pos              := (Cyan         shl 4) + Blue;
  ViewAscii.Env.Insert           := (Cyan         shl 4) + LightMagenta;
  ViewAscii.Env.Modified         := (Cyan         shl 4) + LightRed;
  HexView.Window.Title        := (Blue         shl 4) + Yellow;
  HexView.Window.Border       := (Blue         shl 4) + White;
  HexView.Window.Icon         := (Black        shl 4) + White;
  HexView.Window.Sel          := (Magenta      shl 4) + Yellow;
  HexView.Window.High         := (Blue         shl 4) + LightCyan;
  HexView.Window.Key          := (Cyan         shl 4) + White;
  HexView.Window.kShade       := (Blue         shl 4) + Black;
  HexView.Window.kSel         := (Cyan         shl 4) + Yellow;
  HexView.Env.Input         := (Blue         shl 4) + White;
  EditButt.Title  := (Black        shl 4) + Yellow;
  EditButt.Border := (Black        shl 4) + White;
  EditButt.Icon   := (Black        shl 4) + White;
  EditButt.Sel    := (Magenta      shl 4) + Yellow;
  EditButt.High   := (LightBlue    shl 4) + LightCyan;
  EditButt.Key    := (Cyan         shl 4) + White;
  EditButt.kShade := (Black        shl 4) + Black;
  EditButt.kSel   := (Cyan         shl 4) + Yellow;
  Dial.Window.Title  := (Blue         shl 4) + Yellow;
  Dial.Window.Border := (Blue         shl 4) + White;
  Dial.Window.Icon   := (Black        shl 4) + White;
  Dial.Window.Sel    := (Magenta      shl 4) + Yellow;
  Dial.Window.High   := (Blue         shl 4) + LightCyan;
  Dial.Window.Key    := (Cyan         shl 4) + White;
  Dial.Window.kShade := (Blue         shl 4) + Black;
  Dial.Window.kSel   := (Cyan         shl 4) + Yellow;
  Dial.Env.BarUnselect    := (Blue         shl 4) + LightCyan;
  Dial.Env.BarSelect      := (Magenta      shl 4) + Yellow;
  Draw.Window.Title  := (Black        shl 4) + Yellow;
  Draw.Window.Border := (Black        shl 4) + White;
  Draw.Window.Icon   := (Black        shl 4) + White;
  Draw.Window.Sel    := (Magenta      shl 4) + Yellow;
  Draw.Window.High   := (Black        shl 4) + LightCyan;
  Draw.Window.Key    := (Cyan         shl 4) + White;
  Draw.Window.kShade := (Black        shl 4) + Black;
  Draw.Window.kSel   := (Cyan         shl 4) + Yellow;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (LightMagenta shl 4) + Black;
  FileManager.Window.Border:= (Magenta      shl 4) + White;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (Blue         shl 4) + White;
  FileManager.Window.High  := (Magenta      shl 4) + LightCyan;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (Magenta      shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.SubTitle   := (White        shl 4) + Black;
 End;
 With Colors[MtxBleuet]do Begin
  LastBar.Normal     := (Blue         shl 4) + White;
  LastBar.Higher     := (Blue         shl 4) + LightCyan;
  Menu.Normal        := (LightCyan    shl 4) + Black;
  Menu.High          := (LightCyan    shl 4) + LightRed;
  Menu.Select        := (Black        shl 4) + LightCyan;
  Desktop.Tapiserie         := (Blue         shl 4) + LightBlue;
  Desktop.DialStatus        := (LightGray    shl 4) + Black;
  Dialog.Window.Title  := (White        shl 4) + Black;
  Dialog.Window.Border := (LightBlue    shl 4) + White;
  Dialog.Window.Icon   := (Black        shl 4) + White;
  Dialog.Window.Sel    := (Blue         shl 4) + White;
  Dialog.Window.High   := (LightBlue    shl 4) + LightCyan;
  Dialog.Window.Key    := (White        shl 4) + Black;
  Dialog.Window.kShade := (LightBlue    shl 4) + Black;
  Dialog.Window.kSel   := (White        shl 4) + LightRed;
  Dialog.Env.BarSelInactive := (Blue         shl 4) + White;
  Dialog.Env.BarSelect      := (DarkGray     shl 4) + White;
  Dialog.Env.List.Title  := (White        shl 4) + Black;
  Dialog.Env.List.Border := (LightCyan    shl 4) + Black;
  Dialog.Env.List.Icon   := (Black        shl 4) + White;
  Dialog.Env.List.Sel    := (Blue         shl 4) + White;
  Dialog.Env.List.High   := (LightCyan    shl 4) + LightRed;
  Dialog.Env.List.Key    := (White        shl 4) + Black;
  Dialog.Env.List.kShade := (LightCyan    shl 4) + Black;
  Dialog.Env.List.kSel   := (White        shl 4) + LightRed;
  ErrorWin.Window.Border      := (LightRed     shl 4) + White;
  ErrorWin.Window.Title       := (White        shl 4) + LightBlue;
  ErrorWin.Window.Msg         := (LightRed     shl 4) + Yellow;
  ErrorWin.Window.KeyActif    := (White        shl 4) + LightRed;
  ErrorWin.Window.KeyUnactif  := (White        shl 4) + Black;
  ErrorWin.Window.KeyShade    := (LightRed     shl 4) + DarkGray;
  ErrorWin.Env.Input        := (LightRed     shl 4) + Red;
  WarningWin.Border         := (Yellow       shl 4) + White;
  WarningWin.Title          := (White        shl 4) + LightBlue;
  WarningWin.Msg            := (Yellow       shl 4) + Black;
  WarningWin.KeyActif       := (White        shl 4) + LightRed;
  WarningWin.KeyUnactif     := (White        shl 4) + Black;
  WarningWin.KeyShade       := (Yellow       shl 4) + DarkGray;
  RemWin.Border             := (LightBlue    shl 4) + White;
  RemWin.Title              := (White        shl 4) + Black;
  RemWin.Msg                := (LightBlue    shl 4) + LightCyan;
  RemWin.KeyActif           := (White        shl 4) + LightRed;
  RemWin.KeyUnactif         := (White        shl 4) + Black;
  RemWin.KeyShade           := (LightBlue    shl 4) + Black;
  OpenWin.Window.Title        := (White        shl 4) + Black;
  OpenWin.Window.Border       := (LightBlue    shl 4) + White;
  OpenWin.Window.Icon         := (Black        shl 4) + White;
  OpenWin.Window.Sel          := (Blue         shl 4) + White;
  OpenWin.Window.High         := (LightBlue    shl 4) + LightCyan;
  OpenWin.Window.Key          := (White        shl 4) + Black;
  OpenWin.Window.kShade       := (LightBlue    shl 4) + Black;
  OpenWin.Window.kSel         := (White        shl 4) + LightRed;
  OpenWin.Env.Input         := (Blue         shl 4) + LightCyan;
  OpenWin.Env.Dir           := (Blue         shl 4) + Yellow;
  MalteDos.Window.Title       := (White        shl 4) + Black;
  MalteDos.Window.Border      := (Black        shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Black        shl 4) + LightBlue;
  MalteDos.Window.High        := (Black        shl 4) + LightCyan;
  MalteDos.Window.Key         := (White        shl 4) + Black;
  MalteDos.Window.kShade      := (Black        shl 4) + Black;
  MalteDos.Window.kSel        := (White        shl 4) + LightRed;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Icon          := (Black        shl 4) + White;
  Editor.Window.Border        := (Blue         shl 4) + White;
  Editor.Window.Title         := (White        shl 4) + Black;
  Editor.Window.Sel           := (Blue         shl 4) + White;
  Editor.Window.High          := (LightBlue    shl 4) + LightCyan;
  Editor.Window.kSel          := (White        shl 4) + LightRed;
  Editor.Window.Key           := (White        shl 4) + Black;
  Editor.Window.kShade        := (LightBlue    shl 4) + Black;
  Editor.Env.BarInfo        := (White        shl 4) + Black;
  Editor.Env.Default        := (Blue         shl 4) + LightMagenta;
  Editor.Env.Typewriter     := (Blue         shl 4) + LightGray;
  Editor.Env.Rem            := (Blue         shl 4) + Yellow;
  Editor.Env.ResWord        := (Blue         shl 4) + White;
  Editor.Env.Chars          := (Blue         shl 4) + LightRed;
  Editor.Env.Symbol         := (Blue         shl 4) + LightCyan;
  Editor.Env.AsmInst        := (Blue         shl 4) + LightGreen;
  Editor.Env.Number         := (Blue         shl 4) + LightBlue;
  Editor.Env.Z              := (LightMagenta shl 4) + Blue;
  Editor.Env.Normal         := (Blue         shl 4) + White;
  Editor.Env.Pos            := (White        shl 4) + Blue;
  Editor.Env.Insert         := (White        shl 4) + LightMagenta;
  Editor.Env.Modified       := (White        shl 4) + LightRed;
  Editor.OutZone            := (LightBlue    shl 4) + Black;
  PersonalJournal.Window.Title  := (LightMagenta shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (LightMagenta shl 4) + White;
  PersonalJournal.Window.High   := (Blue         shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (White        shl 4) + Black;
  PersonalJournal.Window.kShade := (Blue         shl 4) + Black;
  PersonalJournal.Window.kSel   := (White        shl 4) + LightRed;
  PersonalJournal.Env.BarInfo := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Default := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos     := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Insert  := (LightMagenta shl 4) + Magenta;
  ViewAscii.Window.Border := (LightBlue    shl 4) + White;
  ViewAscii.Window.Title  := (White        shl 4) + Black;
  ViewAscii.Window.Sel    := (Blue         shl 4) + White;
  ViewAscii.Window.High   := (LightBlue    shl 4) + LightCyan;
  ViewAscii.Window.kSel   := (White        shl 4) + LightRed;
  ViewAscii.Window.Key    := (White        shl 4) + Black;
  ViewAscii.Window.kShade := (LightBlue    shl 4) + Black;
  ViewAscii.Env.BarInfo          := (LightCyan    shl 4) + Black;
  ViewAscii.Env.Default          := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Rem              := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord          := (Blue         shl 4) + White;
  ViewAscii.Env.Chars            := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol           := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst          := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number           := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z                := (LightMagenta shl 4) + Blue;
  ViewAscii.Env.Normal           := (Blue         shl 4) + White;
  ViewAscii.Env.Pos              := (LightCyan    shl 4) + Blue;
  ViewAscii.Env.Insert           := (LightCyan    shl 4) + LightMagenta;
  ViewAscii.Env.Modified         := (LightCyan    shl 4) + LightRed;
  HexView.Window.Title  := (White        shl 4) + Black;
  HexView.Window.Border := (LightBlue    shl 4) + White;
  HexView.Window.Icon   := (Black        shl 4) + White;
  HexView.Window.Sel    := (Blue         shl 4) + White;
  HexView.Window.High   := (LightBlue    shl 4) + LightCyan;
  HexView.Window.Key    := (White        shl 4) + Black;
  HexView.Window.kShade := (LightBlue    shl 4) + Black;
  HexView.Window.kSel   := (White        shl 4) + LightRed;
  HexView.Env.Input   := (Blue         shl 4) + White;
  EditButt.Title  := (White        shl 4) + Black;
  EditButt.Border := (LightBlue    shl 4) + White;
  EditButt.Icon   := (Black        shl 4) + White;
  EditButt.Sel    := (Blue         shl 4) + White;
  EditButt.High   := (LightBlue    shl 4) + LightCyan;
  EditButt.Key    := (White        shl 4) + Black;
  EditButt.kShade := (LightBlue    shl 4) + Black;
  EditButt.kSel   := (White        shl 4) + LightRed;
  Dial.Window.Title  := (White        shl 4) + Black;
  Dial.Window.Border := (LightBlue    shl 4) + White;
  Dial.Window.Icon   := (Black        shl 4) + White;
  Dial.Window.Sel    := (Blue         shl 4) + White;
  Dial.Window.High   := (LightBlue    shl 4) + LightCyan;
  Dial.Window.Key    := (White        shl 4) + Black;
  Dial.Window.kShade := (LightBlue    shl 4) + Black;
  Dial.Window.kSel   := (White        shl 4) + LightRed;
  Dial.Env.BarUnselect := (LightBlue    shl 4) + White;
  Dial.Env.BarSelect   := (Blue         shl 4) + White;
  Draw.Window.Title  := (White        shl 4) + Black;
  Draw.Window.Border := (LightBlue    shl 4) + White;
  Draw.Window.Icon   := (Black        shl 4) + White;
  Draw.Window.Sel    := (Blue         shl 4) + White;
  Draw.Window.High   := (LightBlue    shl 4) + LightCyan;
  Draw.Window.Key    := (White        shl 4) + Black;
  Draw.Window.kShade := (LightBlue    shl 4) + Black;
  Draw.Window.kSel   := (White        shl 4) + LightRed;
  Help.Window.Title  := (White        shl 4) + Black;
  Help.Window.Border := (Black        shl 4) + White;
  Help.Window.Icon   := (Black        shl 4) + White;
  Help.Window.Sel    := (Black        shl 4) + LightBlue;
  Help.Window.High   := (Black        shl 4) + LightCyan;
  Help.Window.Key    := (White        shl 4) + Black;
  Help.Window.kShade := (Black        shl 4) + Black;
  Help.Window.kSel   := (White        shl 4) + LightRed;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (LightMagenta shl 4) + White;
  FileManager.Window.Border:= (Magenta      shl 4) + White;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (Blue         shl 4) + White;
  FileManager.Window.High  := (Magenta      shl 4) + LightCyan;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (Magenta      shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.SubTitle   := (White        shl 4) + Black;
 End;
 With Colors[MtxMagenta]do Begin
  LastBar.Normal     := (Magenta      shl 4) + White;
  LastBar.Higher     := (Magenta      shl 4) + LightCyan;
  Menu.Normal        := (Cyan         shl 4) + White;
  Menu.High          := (Cyan         shl 4) + LightCyan;
  Menu.Select        := (Magenta      shl 4) + Yellow;
  Desktop.Tapiserie         := (Blue         shl 4) + LightBlue;
  Desktop.DialStatus        := (LightGray    shl 4) + Black;
  Dialog.Window.Title  := (Green        shl 4) + Yellow;
  Dialog.Window.Border := (Green        shl 4) + White;
  Dialog.Window.Icon   := (Green        shl 4) + LightGreen;
  Dialog.Window.Sel    := (Magenta      shl 4) + Yellow;
  Dialog.Window.High   := (Green        shl 4) + LightCyan;
  Dialog.Window.Key    := (Cyan         shl 4) + White;
  Dialog.Window.kShade := (Green        shl 4) + Black;
  Dialog.Window.kSel   := (Cyan         shl 4) + Yellow;
  Dialog.Env.BarSelInactive := (Magenta      shl 4) + White;
  Dialog.Env.BarSelect      := (Magenta      shl 4) + Yellow;
  Dialog.Env.List.Title  := (Green        shl 4) + Yellow;
  Dialog.Env.List.Border := (Green        shl 4) + White;
  Dialog.Env.List.Icon   := (Green        shl 4) + LightGreen;
  Dialog.Env.List.Sel    := (Magenta      shl 4) + Yellow;
  Dialog.Env.List.High   := (Green        shl 4) + LightCyan;
  Dialog.Env.List.Key    := (Cyan         shl 4) + White;
  Dialog.Env.List.kShade := (Green        shl 4) + Black;
  Dialog.Env.List.kSel   := (Cyan         shl 4) + Yellow;
  ErrorWin.Window.Border      := (Red          shl 4) + White;
  ErrorWin.Window.Title       := (Cyan         shl 4) + Yellow;
  ErrorWin.Window.Msg         := (Red          shl 4) + Yellow;
  ErrorWin.Window.KeyActif    := (Cyan         shl 4) + Yellow;
  ErrorWin.Window.KeyUnactif  := (Cyan         shl 4) + White;
  ErrorWin.Window.KeyShade    := (Red          shl 4) + Black;
  ErrorWin.Env.Input        := (Blue         shl 4) + LightCyan;
  WarningWin.Border         := (Brown        shl 4) + White;
  WarningWin.Title          := (Brown        shl 4) + Yellow;
  WarningWin.Msg            := (Brown        shl 4) + Yellow;
  WarningWin.KeyActif       := (Cyan         shl 4) + Yellow;
  WarningWin.KeyUnactif     := (Cyan         shl 4) + White;
  WarningWin.KeyShade       := (Brown        shl 4) + Black;
  RemWin.Border             := (Green        shl 4) + White;
  RemWin.Title              := (Green        shl 4) + Yellow;
  RemWin.Msg                := (Green        shl 4) + LightCyan;
  RemWin.KeyActif           := (Cyan         shl 4) + Yellow;
  RemWin.KeyUnactif         := (Cyan         shl 4) + White;
  RemWin.KeyShade           := (Green        shl 4) + Black;
  OpenWin.Window.Title        := (Green        shl 4) + Yellow;
  OpenWin.Window.Border       := (Green        shl 4) + White;
  OpenWin.Window.Icon         := (Green        shl 4) + LightGreen;
  OpenWin.Window.Sel          := (Magenta      shl 4) + Yellow;
  OpenWin.Window.High         := (Green        shl 4) + LightCyan;
  OpenWin.Window.Key          := (Cyan         shl 4) + White;
  OpenWin.Window.kShade       := (Green        shl 4) + Black;
  OpenWin.Window.kSel         := (Cyan         shl 4) + Yellow;
  OpenWin.Env.Input         := (Blue         shl 4) + White;
  OpenWin.Env.Dir           := (Blue         shl 4) + Yellow;
  MalteDos.Window.Title       := (Black        shl 4) + Yellow;
  MalteDos.Window.Border      := (Black        shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Black        shl 4) + LightCyan;
  MalteDos.Window.High        := (Black        shl 4) + LightBlue;
  MalteDos.Window.Key         := (Cyan         shl 4) + White;
  MalteDos.Window.kShade      := (Black        shl 4) + Black;
  MalteDos.Window.kSel        := (Cyan         shl 4) + Yellow;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Border        := (Blue         shl 4) + White;
  Editor.Window.Title         := (Blue         shl 4) + Yellow;
  Editor.Window.Sel           := (Magenta      shl 4) + Yellow;
  Editor.Window.High          := (Blue         shl 4) + LightCyan;
  Editor.Window.kSel          := (Cyan         shl 4) + Yellow;
  Editor.Window.Key           := (Cyan         shl 4) + White;
  Editor.Window.kShade        := (Blue         shl 4) + Black;
  Editor.Env.BarInfo        := (LightCyan    shl 4) + Black;
  Editor.Env.Default        := (Blue         shl 4) + LightMagenta;
  Editor.Env.Typewriter     := (Blue         shl 4) + LightGray;
  Editor.Env.Rem            := (Blue         shl 4) + Yellow;
  Editor.Env.ResWord        := (Blue         shl 4) + White;
  Editor.Env.Chars          := (Blue         shl 4) + LightRed;
  Editor.Env.Symbol         := (Blue         shl 4) + LightCyan;
  Editor.Env.AsmInst        := (Blue         shl 4) + LightGreen;
  Editor.Env.Number         := (Blue         shl 4) + LightBlue;
  Editor.Env.Z              := (Magenta      shl 4) + Blue;
  Editor.Env.Normal         := (Blue         shl 4) + White;
  Editor.Env.Pos            := (Cyan         shl 4) + Blue;
  Editor.Env.Insert         := (Cyan         shl 4) + LightMagenta;
  Editor.Env.Modified       := (Cyan         shl 4) + LightRed;
  Editor.OutZone            := (Magenta      shl 4) + Black;
  PersonalJournal.Window.Title  := (Magenta      shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (Blue         shl 4) + White;
  PersonalJournal.Window.High   := (Magenta      shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (Cyan         shl 4) + White;
  PersonalJournal.Window.kShade := (Magenta      shl 4) + Black;
  PersonalJournal.Window.kSel   := (Cyan         shl 4) + Yellow;
  PersonalJournal.Env.BarInfo    := (Red          shl 4) + Magenta;
  PersonalJournal.Env.Default    := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos        := (Red          shl 4) + Magenta;
  PersonalJournal.Env.Insert     := (Red          shl 4) + Magenta;
  ViewAscii.Window.Border          := (Blue         shl 4) + White;
  ViewAscii.Window.Title           := (Blue         shl 4) + Yellow;
  ViewAscii.Window.Sel             := (Magenta      shl 4) + White;
  ViewAscii.Window.High            := (Blue         shl 4) + LightCyan;
  ViewAscii.Window.kSel            := (Cyan         shl 4) + Yellow;
  ViewAscii.Window.Key             := (Cyan         shl 4) + White;
  ViewAscii.Window.kShade          := (Blue         shl 4) + Black;
  ViewAscii.Env.BarInfo          := (Cyan         shl 4) + Black;
  ViewAscii.Env.Default          := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Rem              := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord          := (Blue         shl 4) + White;
  ViewAscii.Env.Chars            := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol           := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst          := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number           := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z                := (Magenta      shl 4) + Blue;
  ViewAscii.Env.Normal           := (Blue         shl 4) + White;
  ViewAscii.Env.Pos              := (Cyan         shl 4) + Blue;
  ViewAscii.Env.Insert           := (Cyan         shl 4) + LightMagenta;
  ViewAscii.Env.Modified         := (Cyan         shl 4) + LightRed;
  HexView.Window.Title        := (Blue         shl 4) + Yellow;
  HexView.Window.Border       := (Blue         shl 4) + White;
  HexView.Window.Icon         := (Black        shl 4) + White;
  HexView.Window.Sel          := (Magenta      shl 4) + Yellow;
  HexView.Window.High         := (Blue         shl 4) + LightCyan;
  HexView.Window.Key          := (Cyan         shl 4) + White;
  HexView.Window.kShade       := (Blue         shl 4) + Black;
  HexView.Window.kSel         := (Cyan         shl 4) + Yellow;
  HexView.Env.Input         := (Blue         shl 4) + White;
  EditButt.Title  := (Black        shl 4) + Yellow;
  EditButt.Border := (Black        shl 4) + White;
  EditButt.Icon   := (Black        shl 4) + White;
  EditButt.Sel    := (Magenta      shl 4) + Yellow;
  EditButt.High   := (Blue         shl 4) + LightCyan;
  EditButt.Key    := (Cyan         shl 4) + White;
  EditButt.kShade := (Black        shl 4) + Black;
  EditButt.kSel   := (Cyan         shl 4) + Yellow;
  Dial.Window.Title  := (Blue         shl 4) + Yellow;
  Dial.Window.Border := (Blue         shl 4) + White;
  Dial.Window.Icon   := (Black        shl 4) + White;
  Dial.Window.Sel    := (Magenta      shl 4) + Yellow;
  Dial.Window.High   := (Blue         shl 4) + LightCyan;
  Dial.Window.Key    := (Cyan         shl 4) + White;
  Dial.Window.kShade := (Blue         shl 4) + Black;
  Dial.Window.kSel   := (Cyan         shl 4) + Yellow;
  Dial.Env.BarUnselect    := (Blue         shl 4) + LightCyan;
  Dial.Env.BarSelect      := (Magenta      shl 4) + Yellow;
  Draw.Window.Title  := (Black        shl 4) + Yellow;
  Draw.Window.Border := (Black        shl 4) + White;
  Draw.Window.Icon   := (Black        shl 4) + White;
  Draw.Window.Sel    := (Magenta      shl 4) + Yellow;
  Draw.Window.High   := (Black        shl 4) + LightCyan;
  Draw.Window.Key    := (Cyan         shl 4) + White;
  Draw.Window.kShade := (Black        shl 4) + Black;
  Draw.Window.kSel   := (Cyan         shl 4) + Yellow;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (LightMagenta shl 4) + Black;
  FileManager.Window.Border:= (Magenta      shl 4) + White;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (Blue         shl 4) + White;
  FileManager.Window.High  := (Magenta      shl 4) + LightCyan;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (Magenta      shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.SubTitle   := (White        shl 4) + Black;
 End;
 With Colors[MtxDiablo]do Begin
  LastBar.Normal     := (Red          shl 4) + Black;
  LastBar.Higher     := (Red          shl 4) + LightRed;
  Menu.Normal        := (LightRed     shl 4) + Black;
  Menu.High          := (LightRed     shl 4) + Red;
  Menu.Select        := (Red          shl 4) + LightRed;
  Desktop.Tapiserie         := (Red          shl 4) + LightRed;
  Desktop.DialStatus        := (LightRed     shl 4) + Black;
  Dialog.Window.Title  := (White        shl 4) + Black;
  Dialog.Window.Border := (LightRed     shl 4) + White;
  Dialog.Window.Icon   := (Black        shl 4) + White;
  Dialog.Window.Sel    := (Black        shl 4) + White;
  Dialog.Window.High   := (LightRed     shl 4) + Yellow;
  Dialog.Window.Key    := (White        shl 4) + Black;
  Dialog.Window.kShade := (LightRed     shl 4) + Black;
  Dialog.Window.kSel   := (White        shl 4) + LightRed;
  Dialog.Env.BarSelInactive := (Black        shl 4) + White;
  Dialog.Env.BarSelect      := (DarkGray     shl 4) + White;
  Dialog.Env.List.Title  := (White        shl 4) + Black;
  Dialog.Env.List.Border := (LightRed     shl 4) + White;
  Dialog.Env.List.Icon   := (Black        shl 4) + White;
  Dialog.Env.List.Sel    := (Black        shl 4) + White;
  Dialog.Env.List.High   := (LightRed     shl 4) + Yellow;
  Dialog.Env.List.Key    := (White        shl 4) + Black;
  Dialog.Env.List.kShade := (LightRed     shl 4) + Black;
  Dialog.Env.List.kSel   := (White        shl 4) + LightRed;
  ErrorWin.Window.Border      := (LightRed     shl 4) + White;
  ErrorWin.Window.Title       := (White        shl 4) + LightBlue;
  ErrorWin.Window.Msg         := (LightRed     shl 4) + Yellow;
  ErrorWin.Window.KeyActif    := (White        shl 4) + LightRed;
  ErrorWin.Window.KeyUnactif  := (White        shl 4) + Black;
  ErrorWin.Window.KeyShade    := (LightRed     shl 4) + DarkGray;
  ErrorWin.Env.Input        := (LightRed     shl 4) + Red;
  WarningWin.Border         := (Yellow       shl 4) + White;
  WarningWin.Title          := (White        shl 4) + LightBlue;
  WarningWin.Msg            := (Yellow       shl 4) + Black;
  WarningWin.KeyActif       := (White        shl 4) + LightRed;
  WarningWin.KeyUnactif     := (White        shl 4) + Black;
  WarningWin.KeyShade       := (Yellow       shl 4) + DarkGray;
  RemWin.Border             := (LightBlue    shl 4) + White;
  RemWin.Title              := (White        shl 4) + Black;
  RemWin.Msg                := (LightBlue    shl 4) + LightCyan;
  RemWin.KeyActif           := (White        shl 4) + LightRed;
  RemWin.KeyUnactif         := (White        shl 4) + Black;
  RemWin.KeyShade           := (LightBlue    shl 4) + Black;
  OpenWin.Window.Title        := (White        shl 4) + Black;
  OpenWin.Window.Border       := (LightRed     shl 4) + White;
  OpenWin.Window.Icon         := (Black        shl 4) + White;
  OpenWin.Window.Sel          := (Red          shl 4) + White;
  OpenWin.Window.High         := (LightRed     shl 4) + LightCyan;
  OpenWin.Window.Key          := (White        shl 4) + Black;
  OpenWin.Window.kShade       := (LightRed     shl 4) + Black;
  OpenWin.Window.kSel         := (White        shl 4) + LightRed;
  OpenWin.Env.Input         := (Red          shl 4) + LightCyan;
  OpenWin.Env.Dir           := (Red          shl 4) + Yellow;
  MalteDos.Window.Title       := (White        shl 4) + Black;
  MalteDos.Window.Border      := (Black        shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Black        shl 4) + LightBlue;
  MalteDos.Window.High        := (Black        shl 4) + LightCyan;
  MalteDos.Window.Key         := (White        shl 4) + Black;
  MalteDos.Window.kShade      := (Black        shl 4) + Black;
  MalteDos.Window.kSel        := (White        shl 4) + LightRed;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Icon          := (Black        shl 4) + White;
  Editor.Window.Border        := (Black        shl 4) + White;
  Editor.Window.Title         := (White        shl 4) + Black;
  Editor.Window.Sel           := (Black        shl 4) + White;
  Editor.Window.High          := (Yellow       shl 4) + Black;
  Editor.Window.kSel          := (White        shl 4) + LightRed;
  Editor.Window.Key           := (White        shl 4) + Black;
  Editor.Window.kShade        := (LightBlue    shl 4) + Black;
  Editor.Env.BarInfo        := (White        shl 4) + Black;
  Editor.Env.Default        := (Black        shl 4) + LightMagenta;
  Editor.Env.Typewriter     := (Black        shl 4) + LightGray;
  Editor.Env.Rem            := (Black        shl 4) + Yellow;
  Editor.Env.ResWord        := (Black        shl 4) + White;
  Editor.Env.Chars          := (Black        shl 4) + LightRed;
  Editor.Env.Symbol         := (Black        shl 4) + LightCyan;
  Editor.Env.AsmInst        := (Black        shl 4) + LightGreen;
  Editor.Env.Number         := (Black        shl 4) + LightBlue;
  Editor.Env.Z              := (LightMagenta shl 4) + Blue;
  Editor.Env.Normal         := (Black        shl 4) + White;
  Editor.Env.Pos            := (White        shl 4) + Black;
  Editor.Env.Insert         := (White        shl 4) + LightMagenta;
  Editor.Env.Modified       := (White        shl 4) + LightRed;
  Editor.OutZone            := (Yellow       shl 4) + Black;
  PersonalJournal.Window.Title  := (LightMagenta shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (LightMagenta shl 4) + White;
  PersonalJournal.Window.High   := (Blue         shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (White        shl 4) + Black;
  PersonalJournal.Window.kShade := (Blue         shl 4) + Black;
  PersonalJournal.Window.kSel   := (White        shl 4) + LightRed;
  PersonalJournal.Env.BarInfo := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Default := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos     := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Insert  := (LightMagenta shl 4) + Magenta;
  ViewAscii.Window.Border := (LightBlue    shl 4) + White;
  ViewAscii.Window.Title  := (White        shl 4) + Black;
  ViewAscii.Window.Sel    := (Blue         shl 4) + White;
  ViewAscii.Window.High   := (LightBlue    shl 4) + LightCyan;
  ViewAscii.Window.kSel   := (White        shl 4) + LightRed;
  ViewAscii.Window.Key    := (White        shl 4) + Black;
  ViewAscii.Window.kShade := (LightBlue    shl 4) + Black;
  ViewAscii.Env.BarInfo          := (LightCyan    shl 4) + Black;
  ViewAscii.Env.Default          := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Rem              := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord          := (Blue         shl 4) + White;
  ViewAscii.Env.Chars            := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol           := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst          := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number           := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z                := (LightMagenta shl 4) + Blue;
  ViewAscii.Env.Normal           := (Blue         shl 4) + White;
  ViewAscii.Env.Pos              := (LightCyan    shl 4) + Blue;
  ViewAscii.Env.Insert           := (LightCyan    shl 4) + LightMagenta;
  ViewAscii.Env.Modified         := (LightCyan    shl 4) + LightRed;
  HexView.Window.Title  := (White        shl 4) + Black;
  HexView.Window.Border := (LightBlue    shl 4) + White;
  HexView.Window.Icon   := (Black        shl 4) + White;
  HexView.Window.Sel    := (Blue         shl 4) + White;
  HexView.Window.High   := (LightBlue    shl 4) + LightCyan;
  HexView.Window.Key    := (White        shl 4) + Black;
  HexView.Window.kShade := (LightBlue    shl 4) + Black;
  HexView.Window.kSel   := (White        shl 4) + LightRed;
  HexView.Env.Input   := (Blue         shl 4) + White;
  EditButt.Title  := (White        shl 4) + Black;
  EditButt.Border := (LightBlue    shl 4) + White;
  EditButt.Icon   := (Black        shl 4) + White;
  EditButt.Sel    := (Blue         shl 4) + White;
  EditButt.High   := (LightBlue    shl 4) + LightCyan;
  EditButt.Key    := (White        shl 4) + Black;
  EditButt.kShade := (LightBlue    shl 4) + Black;
  EditButt.kSel   := (White        shl 4) + LightRed;
  Dial.Window.Title  := (White        shl 4) + Black;
  Dial.Window.Border := (LightRed     shl 4) + White;
  Dial.Window.Icon   := (Black        shl 4) + White;
  Dial.Window.Sel    := (Red          shl 4) + Yellow;
  Dial.Window.High   := (LightRed     shl 4) + LightCyan;
  Dial.Window.Key    := (White        shl 4) + Black;
  Dial.Window.kShade := (LightRed     shl 4) + Black;
  Dial.Window.kSel   := (White        shl 4) + LightRed;
  Dial.Env.BarUnselect := (Yellow       shl 4) + Black;
  Dial.Env.BarSelect   := (Black        shl 4) + White;
  Draw.Window.Title  := (White        shl 4) + Black;
  Draw.Window.Border := (LightBlue    shl 4) + White;
  Draw.Window.Icon   := (Black        shl 4) + White;
  Draw.Window.Sel    := (Blue         shl 4) + White;
  Draw.Window.High   := (LightBlue    shl 4) + LightCyan;
  Draw.Window.Key    := (White        shl 4) + Black;
  Draw.Window.kShade := (LightBlue    shl 4) + Black;
  Draw.Window.kSel   := (White        shl 4) + LightRed;
  Help.Window.Title  := (White        shl 4) + Black;
  Help.Window.Border := (Black        shl 4) + White;
  Help.Window.Icon   := (Black        shl 4) + White;
  Help.Window.Sel    := (Black        shl 4) + LightBlue;
  Help.Window.High   := (Black        shl 4) + LightCyan;
  Help.Window.Key    := (White        shl 4) + Black;
  Help.Window.kShade := (Black        shl 4) + Black;
  Help.Window.kSel   := (White        shl 4) + LightRed;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (LightMagenta shl 4) + White;
  FileManager.Window.Border:= (Magenta      shl 4) + White;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (Blue         shl 4) + White;
  FileManager.Window.High  := (Magenta      shl 4) + LightCyan;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (Magenta      shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.SubTitle   := (White        shl 4) + Black;
 End;
 With Colors[MtxGraphix]do Begin
  LastBar.Normal     := (Blue         shl 4) + White;
  LastBar.Higher     := (Blue         shl 4) + LightCyan;
  Menu.Normal        := (White        shl 4) + Black;
  Menu.High          := (White        shl 4) + LightRed;
  Menu.Select        := (Black        shl 4) + LightCyan;
  Desktop.Tapiserie         := (Blue         shl 4) + LightBlue;
  Desktop.DialStatus        := (LightCyan    shl 4) + Cyan;
  Dialog.Window.Title  := (White        shl 4) + Black;
  Dialog.Window.Border := (LightBlue    shl 4) + White;
  Dialog.Window.Icon   := (Black        shl 4) + White;
  Dialog.Window.Sel    := (Blue         shl 4) + White;
  Dialog.Window.High   := (LightBlue    shl 4) + LightCyan;
  Dialog.Window.Key    := (White        shl 4) + Black;
  Dialog.Window.kShade := (LightBlue    shl 4) + Black;
  Dialog.Window.kSel   := (White        shl 4) + LightRed;
  Dialog.Env.BarSelInactive := (Blue         shl 4) + White;
  Dialog.Env.BarSelect      := (DarkGray     shl 4) + White;
  Dialog.Env.List.Title  := (White        shl 4) + Black;
  Dialog.Env.List.Border := (LightBlue    shl 4) + White;
  Dialog.Env.List.Icon   := (Black        shl 4) + White;
  Dialog.Env.List.Sel    := (Blue         shl 4) + White;
  Dialog.Env.List.High   := (LightBlue    shl 4) + LightCyan;
  Dialog.Env.List.Key    := (White        shl 4) + Black;
  Dialog.Env.List.kShade := (LightBlue    shl 4) + Black;
  Dialog.Env.List.kSel   := (White        shl 4) + LightRed;
  ErrorWin.Window.Border      := (LightRed     shl 4) + White;
  ErrorWin.Window.Title       := (White        shl 4) + LightBlue;
  ErrorWin.Window.Msg         := (LightRed     shl 4) + Yellow;
  ErrorWin.Window.KeyActif    := (White        shl 4) + LightRed;
  ErrorWin.Window.KeyUnactif  := (White        shl 4) + Black;
  ErrorWin.Window.KeyShade    := (LightRed     shl 4) + DarkGray;
  ErrorWin.Env.Input        := (LightRed     shl 4) + Red;
  WarningWin.Border         := (Yellow       shl 4) + White;
  WarningWin.Title          := (White        shl 4) + LightBlue;
  WarningWin.Msg            := (Yellow       shl 4) + Black;
  WarningWin.KeyActif       := (White        shl 4) + LightRed;
  WarningWin.KeyUnactif     := (White        shl 4) + Black;
  WarningWin.KeyShade       := (Yellow       shl 4) + DarkGray;
  RemWin.Border             := (LightBlue    shl 4) + White;
  RemWin.Title              := (White        shl 4) + Black;
  RemWin.Msg                := (LightBlue    shl 4) + LightCyan;
  RemWin.KeyActif           := (White        shl 4) + LightRed;
  RemWin.KeyUnactif         := (White        shl 4) + Black;
  RemWin.KeyShade           := (LightBlue    shl 4) + Black;
  OpenWin.Window.Title        := (White        shl 4) + Black;
  OpenWin.Window.Border       := (LightBlue    shl 4) + White;
  OpenWin.Window.Icon         := (Black        shl 4) + White;
  OpenWin.Window.Sel          := (Blue         shl 4) + White;
  OpenWin.Window.High         := (LightBlue    shl 4) + LightCyan;
  OpenWin.Window.Key          := (White        shl 4) + Black;
  OpenWin.Window.kShade       := (LightBlue    shl 4) + Black;
  OpenWin.Window.kSel         := (White        shl 4) + LightRed;
  OpenWin.Env.Input         := (Blue         shl 4) + LightCyan;
  OpenWin.Env.Dir           := (Blue         shl 4) + Yellow;
  MalteDos.Window.Title       := (White        shl 4) + Black;
  MalteDos.Window.Border      := (Black        shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Black        shl 4) + LightBlue;
  MalteDos.Window.High        := (Black        shl 4) + LightCyan;
  MalteDos.Window.Key         := (White        shl 4) + Black;
  MalteDos.Window.kShade      := (Black        shl 4) + Black;
  MalteDos.Window.kSel        := (White        shl 4) + LightRed;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Icon          := (Black        shl 4) + White;
  Editor.Window.Border        := (Blue         shl 4) + White;
  Editor.Window.Title         := (White        shl 4) + Black;
  Editor.Window.Sel           := (Blue         shl 4) + White;
  Editor.Window.High          := (LightBlue    shl 4) + LightCyan;
  Editor.Window.kSel          := (White        shl 4) + LightRed;
  Editor.Window.Key           := (White        shl 4) + Black;
  Editor.Window.kShade        := (LightBlue    shl 4) + Black;
  Editor.Env.BarInfo        := (LightGray    shl 4) + Black;
  Editor.Env.Default        := (Blue         shl 4) + LightMagenta;
  Editor.Env.Typewriter     := (Blue         shl 4) + LightGray;
  Editor.Env.Rem            := (Blue         shl 4) + Yellow;
  Editor.Env.ResWord        := (Blue         shl 4) + White;
  Editor.Env.Chars          := (Blue         shl 4) + LightRed;
  Editor.Env.Symbol         := (Blue         shl 4) + LightCyan;
  Editor.Env.AsmInst        := (Blue         shl 4) + LightGreen;
  Editor.Env.Number         := (Blue         shl 4) + LightBlue;
  Editor.Env.Z              := (LightMagenta shl 4) + Blue;
  Editor.Env.Normal         := (Blue         shl 4) + White;
  Editor.Env.Pos            := (LightGray    shl 4) + Blue;
  Editor.Env.Insert         := (LightGray    shl 4) + Yellow;
  Editor.Env.Modified       := (LightGray    shl 4) + LightRed;
  Editor.OutZone            := (LightBlue    shl 4) + Black;
  PersonalJournal.Window.Title  := (LightMagenta shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (LightMagenta shl 4) + White;
  PersonalJournal.Window.High   := (Blue         shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (White        shl 4) + Black;
  PersonalJournal.Window.kShade := (Blue         shl 4) + Black;
  PersonalJournal.Window.kSel   := (White        shl 4) + LightRed;
  PersonalJournal.Env.BarInfo := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Default := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos     := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Insert  := (LightMagenta shl 4) + Magenta;
  ViewAscii.Window.Border := (LightBlue    shl 4) + White;
  ViewAscii.Window.Title  := (White        shl 4) + Black;
  ViewAscii.Window.Sel    := (Blue         shl 4) + White;
  ViewAscii.Window.High   := (LightBlue    shl 4) + LightCyan;
  ViewAscii.Window.kSel   := (White        shl 4) + LightRed;
  ViewAscii.Window.Key    := (White        shl 4) + Black;
  ViewAscii.Window.kShade := (LightBlue    shl 4) + Black;
  ViewAscii.Env.BarInfo          := (LightCyan    shl 4) + Black;
  ViewAscii.Env.Default          := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Rem              := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord          := (Blue         shl 4) + White;
  ViewAscii.Env.Chars            := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol           := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst          := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number           := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z                := (LightMagenta shl 4) + Blue;
  ViewAscii.Env.Normal           := (Blue         shl 4) + White;
  ViewAscii.Env.Pos              := (LightCyan    shl 4) + Blue;
  ViewAscii.Env.Insert           := (LightCyan    shl 4) + LightMagenta;
  ViewAscii.Env.Modified         := (LightCyan    shl 4) + LightRed;
  HexView.Window.Title  := (White        shl 4) + Black;
  HexView.Window.Border := (LightBlue    shl 4) + White;
  HexView.Window.Icon   := (Black        shl 4) + White;
  HexView.Window.Sel    := (Blue         shl 4) + White;
  HexView.Window.High   := (LightBlue    shl 4) + LightCyan;
  HexView.Window.Key    := (White        shl 4) + Black;
  HexView.Window.kShade := (LightBlue    shl 4) + Black;
  HexView.Window.kSel   := (White        shl 4) + LightRed;
  HexView.Env.Input   := (Blue         shl 4) + White;
  EditButt.Title  := (White        shl 4) + Black;
  EditButt.Border := (LightBlue    shl 4) + White;
  EditButt.Icon   := (Black        shl 4) + White;
  EditButt.Sel    := (Blue         shl 4) + White;
  EditButt.High   := (LightBlue    shl 4) + LightCyan;
  EditButt.Key    := (White        shl 4) + Black;
  EditButt.kShade := (LightBlue    shl 4) + Black;
  EditButt.kSel   := (White        shl 4) + LightRed;
  Dial.Window.Title  := (White        shl 4) + Black;
  Dial.Window.Border := (LightBlue    shl 4) + White;
  Dial.Window.Icon   := (Black        shl 4) + White;
  Dial.Window.Sel    := (Blue         shl 4) + White;
  Dial.Window.High   := (LightBlue    shl 4) + LightCyan;
  Dial.Window.Key    := (White        shl 4) + Black;
  Dial.Window.kShade := (LightBlue    shl 4) + Black;
  Dial.Window.kSel   := (White        shl 4) + LightRed;
  Dial.Env.BarUnselect := (LightBlue    shl 4) + White;
  Dial.Env.BarSelect   := (Blue         shl 4) + White;
  Draw.Window.Title  := (White        shl 4) + Black;
  Draw.Window.Border := (LightBlue    shl 4) + White;
  Draw.Window.Icon   := (Black        shl 4) + White;
  Draw.Window.Sel    := (Blue         shl 4) + White;
  Draw.Window.High   := (LightBlue    shl 4) + LightCyan;
  Draw.Window.Key    := (White        shl 4) + Black;
  Draw.Window.kShade := (LightBlue    shl 4) + Black;
  Draw.Window.kSel   := (White        shl 4) + LightRed;
  Help.Window.Title  := (White        shl 4) + Black;
  Help.Window.Border := (Black        shl 4) + White;
  Help.Window.Icon   := (Black        shl 4) + White;
  Help.Window.Sel    := (Black        shl 4) + LightBlue;
  Help.Window.High   := (Black        shl 4) + LightCyan;
  Help.Window.Key    := (White        shl 4) + Black;
  Help.Window.kShade := (Black        shl 4) + Black;
  Help.Window.kSel   := (White        shl 4) + LightRed;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (LightMagenta shl 4) + Black;
  FileManager.Window.Border:= (Magenta      shl 4) + White;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (Blue         shl 4) + White;
  FileManager.Window.High  := (Magenta      shl 4) + LightCyan;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (Magenta      shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.SubTitle   := (White        shl 4) + Black;
 End;
 With Colors[MtxMac]do Begin
  LastBar.Normal     := (Blue         shl 4) + White;
  LastBar.Higher     := (Blue         shl 4) + LightCyan;
  Menu.Normal        := (White        shl 4) + Black;
  Menu.High          := (White        shl 4) + LightBlue;
  Menu.Select        := (Black        shl 4) + White;
  Desktop.Tapiserie         := (LightBlue    shl 4) + White;
  Desktop.DialStatus        := (LightCyan    shl 4) + Black;
  Dialog.Window.Title  := (LightBlue    shl 4) + White;
  Dialog.Window.Border := (White        shl 4) + Blue;
  Dialog.Window.Icon   := (Black        shl 4) + White;
  Dialog.Window.Sel    := (Black        shl 4) + LightBlue;
  Dialog.Window.High   := (White        shl 4) + Cyan;
  Dialog.Window.Key    := (White        shl 4) + Black;
  Dialog.Window.kShade := (White        shl 4) + Black;
  Dialog.Window.kSel   := (White        shl 4) + LightRed;
  Dialog.Env.BarSelInactive := (White        shl 4) + Blue;
  Dialog.Env.BarSelect      := (White        shl 4) + DarkGray;
  Dialog.Env.List.Title  := (LightBlue    shl 4) + White;
  Dialog.Env.List.Border := (White        shl 4) + Blue;
  Dialog.Env.List.Icon   := (Black        shl 4) + White;
  Dialog.Env.List.Sel    := (Black        shl 4) + LightBlue;
  Dialog.Env.List.High   := (White        shl 4) + Cyan;
  Dialog.Env.List.Key    := (White        shl 4) + Black;
  Dialog.Env.List.kShade := (White        shl 4) + Black;
  Dialog.Env.List.kSel   := (White        shl 4) + LightRed;
  ErrorWin.Window.Border      := (White        shl 4) + LightRed;
  ErrorWin.Window.Title       := (LightRed     shl 4) + White;
  ErrorWin.Window.Msg         := (White        shl 4) + Black;
  ErrorWin.Window.KeyActif    := (White        shl 4) + LightRed;
  ErrorWin.Window.KeyUnactif  := (White        shl 4) + Black;
  ErrorWin.Window.KeyShade    := (White        shl 4) + DarkGray;
  ErrorWin.Env.Input        := (LightRed     shl 4) + Red;
  WarningWin.Border         := (Yellow       shl 4) + White;
  WarningWin.Title          := (White        shl 4) + LightBlue;
  WarningWin.Msg            := (Yellow       shl 4) + Black;
  WarningWin.KeyActif       := (White        shl 4) + LightRed;
  WarningWin.KeyUnactif     := (White        shl 4) + Black;
  WarningWin.KeyShade       := (Yellow       shl 4) + DarkGray;
  RemWin.Border             := (LightBlue    shl 4) + White;
  RemWin.Title              := (White        shl 4) + Black;
  RemWin.Msg                := (LightBlue    shl 4) + LightCyan;
  RemWin.KeyActif           := (White        shl 4) + LightRed;
  RemWin.KeyUnactif         := (White        shl 4) + Black;
  RemWin.KeyShade           := (LightBlue    shl 4) + Black;
  OpenWin.Window.Title        := (LightBlue    shl 4) + White;
  OpenWin.Window.Border       := (White        shl 4) + Blue;
  OpenWin.Window.Icon         := (Black        shl 4) + White;
  OpenWin.Window.Sel          := (White        shl 4) + Blue;
  OpenWin.Window.High         := (White        shl 4) + Cyan;
  OpenWin.Window.Key          := (White        shl 4) + Black;
  OpenWin.Window.kShade       := (White        shl 4) + Black;
  OpenWin.Window.kSel         := (White        shl 4) + LightRed;
  OpenWin.Env.Input         := (LightCyan    shl 4) + Cyan;
  OpenWin.Env.Dir           := (LightCyan    shl 4) + Blue;
  MalteDos.Window.Title       := (White        shl 4) + Black;
  MalteDos.Window.Border      := (Black        shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Black        shl 4) + LightBlue;
  MalteDos.Window.High        := (Black        shl 4) + LightCyan;
  MalteDos.Window.Key         := (White        shl 4) + Black;
  MalteDos.Window.kShade      := (Black        shl 4) + Black;
  MalteDos.Window.kSel        := (White        shl 4) + LightRed;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Title  := (LightBlue    shl 4) + White;
  Editor.Window.Border := (White        shl 4) + Blue;
  Editor.Window.Icon   := (Black        shl 4) + White;
  Editor.Window.Sel    := (White        shl 4) + Blue;
  Editor.Window.High   := (White        shl 4) + Cyan;
  Editor.Window.Key    := (White        shl 4) + Black;
  Editor.Window.kShade := (White        shl 4) + Black;
  Editor.Window.kSel   := (White        shl 4) + LightRed;
  Editor.Env.BarInfo        := (LightBlue    shl 4) + Black;
  Editor.Env.Default        := (White        shl 4) + Magenta;
  Editor.Env.Typewriter     := (White        shl 4) + Black;
  Editor.Env.Rem            := (Yellow       shl 4) + Black;
  Editor.Env.ResWord        := (White        shl 4) + Black;
  Editor.Env.Chars          := (White        shl 4) + Red;
  Editor.Env.Symbol         := (White        shl 4) + Cyan;
  Editor.Env.AsmInst        := (White        shl 4) + Green;
  Editor.Env.Number         := (White        shl 4) + Blue;
  Editor.Env.Z              := (LightMagenta shl 4) + Blue;
  Editor.Env.Normal         := (White        shl 4) + Blue;
  Editor.Env.Pos            := (LightBlue    shl 4) + LightCyan;
  Editor.Env.Insert         := (LightBlue    shl 4) + LightMagenta;
  Editor.Env.Modified       := (LightBlue    shl 4) + LightRed;
  Editor.OutZone            := (LightGray    shl 4) + Black;
  PersonalJournal.Window.Title  := (LightMagenta shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (LightMagenta shl 4) + White;
  PersonalJournal.Window.High   := (Blue         shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (White        shl 4) + Black;
  PersonalJournal.Window.kShade := (Blue         shl 4) + Black;
  PersonalJournal.Window.kSel   := (White        shl 4) + LightRed;
  PersonalJournal.Env.BarInfo := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Default := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos     := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Insert  := (LightMagenta shl 4) + Magenta;
  ViewAscii.Window.Border := (LightBlue    shl 4) + White;
  ViewAscii.Window.Title  := (White        shl 4) + Black;
  ViewAscii.Window.Sel    := (Blue         shl 4) + White;
  ViewAscii.Window.High   := (LightBlue    shl 4) + LightCyan;
  ViewAscii.Window.kSel   := (White        shl 4) + LightRed;
  ViewAscii.Window.Key    := (White        shl 4) + Black;
  ViewAscii.Window.kShade := (LightBlue    shl 4) + Black;
  ViewAscii.Env.BarInfo          := (LightCyan    shl 4) + Black;
  ViewAscii.Env.Default          := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Rem              := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord          := (Blue         shl 4) + White;
  ViewAscii.Env.Chars            := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol           := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst          := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number           := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z                := (LightMagenta shl 4) + Blue;
  ViewAscii.Env.Normal           := (Blue         shl 4) + White;
  ViewAscii.Env.Pos              := (LightCyan    shl 4) + Blue;
  ViewAscii.Env.Insert           := (LightCyan    shl 4) + LightMagenta;
  ViewAscii.Env.Modified         := (LightCyan    shl 4) + LightRed;
  HexView.Window.Title  := (White        shl 4) + Black;
  HexView.Window.Border := (LightBlue    shl 4) + White;
  HexView.Window.Icon   := (Black        shl 4) + White;
  HexView.Window.Sel    := (Blue         shl 4) + White;
  HexView.Window.High   := (LightBlue    shl 4) + LightCyan;
  HexView.Window.Key    := (White        shl 4) + Black;
  HexView.Window.kShade := (LightBlue    shl 4) + Black;
  HexView.Window.kSel   := (White        shl 4) + LightRed;
  HexView.Env.Input   := (Blue         shl 4) + White;
  EditButt.Title  := (White        shl 4) + Black;
  EditButt.Border := (LightBlue    shl 4) + White;
  EditButt.Icon   := (Black        shl 4) + White;
  EditButt.Sel    := (Blue         shl 4) + White;
  EditButt.High   := (LightBlue    shl 4) + LightCyan;
  EditButt.Key    := (White        shl 4) + Black;
  EditButt.kShade := (LightBlue    shl 4) + Black;
  EditButt.kSel   := (White        shl 4) + LightRed;
  Dial.Window.Title  := (LightBlue    shl 4) + White;
  Dial.Window.Border := (White        shl 4) + Blue;
  Dial.Window.Icon   := (Black        shl 4) + White;
  Dial.Window.Sel    := (White        shl 4) + Blue;
  Dial.Window.High   := (White        shl 4) + Cyan;
  Dial.Window.Key    := (LightGray    shl 4) + Black;
  Dial.Window.kShade := (White        shl 4) + Black;
  Dial.Window.kSel   := (White        shl 4) + LightRed;
  Dial.Env.BarUnSelect := (White        shl 4) + Blue;
  Dial.Env.BarSelect   := (White        shl 4) + DarkGray;
  Draw.Window.Title  := (White        shl 4) + Black;
  Draw.Window.Border := (LightBlue    shl 4) + White;
  Draw.Window.Icon   := (Black        shl 4) + White;
  Draw.Window.Sel    := (Blue         shl 4) + White;
  Draw.Window.High   := (LightBlue    shl 4) + LightCyan;
  Draw.Window.Key    := (White        shl 4) + Black;
  Draw.Window.kShade := (LightBlue    shl 4) + Black;
  Draw.Window.kSel   := (White        shl 4) + LightRed;
  Help.Window.Title  := (White        shl 4) + Black;
  Help.Window.Border := (Black        shl 4) + White;
  Help.Window.Icon   := (Black        shl 4) + White;
  Help.Window.Sel    := (Black        shl 4) + LightBlue;
  Help.Window.High   := (Black        shl 4) + LightCyan;
  Help.Window.Key    := (White        shl 4) + Black;
  Help.Window.kShade := (Black        shl 4) + Black;
  Help.Window.kSel   := (White        shl 4) + LightRed;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (LightMagenta shl 4) + Black;
  FileManager.Window.Border:= (Magenta      shl 4) + White;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (Blue         shl 4) + White;
  FileManager.Window.High  := (Magenta      shl 4) + LightCyan;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (Magenta      shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.SubTitle   := (White        shl 4) + Black;
 End;
 With Colors[MtxOS2Win95]do Begin
  LastBar.Normal     := (Blue         shl 4) + White;
  LastBar.Higher     := (Blue         shl 4) + LightCyan;
  Menu.Normal        := (LightGray    shl 4) + Black;
  Menu.High          := (LightGray    shl 4) + LightRed;
  Menu.Select        := (LightBlue    shl 4) + White;
  Desktop.Tapiserie         := (Cyan         shl 4) + White;
  Desktop.DialStatus        := (LightGray    shl 4) + Black;
  Dialog.Window.Title  := (LightBlue    shl 4) + LightGray;
  Dialog.Window.Border := (LightGray    shl 4) + Black;
  Dialog.Window.Icon   := (Black        shl 4) + White;
  Dialog.Window.Sel    := (Blue         shl 4) + White;
  Dialog.Window.High   := (LightGray    shl 4) + Cyan;
  Dialog.Window.Key    := (White        shl 4) + Black;
  Dialog.Window.kShade := (LightGray    shl 4) + Black;
  Dialog.Window.kSel   := (White        shl 4) + LightRed;
  Dialog.Env.BarSelInactive := (White        shl 4) + Black;
  Dialog.Env.BarSelect      := (White        shl 4) + DarkGray;
  Dialog.Env.List.Title  := (LightBlue    shl 4) + White;
  Dialog.Env.List.Border := (White        shl 4) + Black;
  Dialog.Env.List.Icon   := (Black        shl 4) + White;
  Dialog.Env.List.Sel    := (Blue         shl 4) + White;
  Dialog.Env.List.High   := (White        shl 4) + Cyan;
  Dialog.Env.List.Key    := (White        shl 4) + Black;
  Dialog.Env.List.kShade := (White        shl 4) + Black;
  Dialog.Env.List.kSel   := (White        shl 4) + LightRed;
  ErrorWin.Window.Border      := (LightGray    shl 4) + LightRed;
  ErrorWin.Window.Title       := (LightRed     shl 4) + LightGray;
  ErrorWin.Window.Msg         := (LightGray    shl 4) + Black;
  ErrorWin.Window.KeyActif    := (White        shl 4) + LightRed;
  ErrorWin.Window.KeyUnactif  := (White        shl 4) + Black;
  ErrorWin.Window.KeyShade    := (LightGray    shl 4) + DarkGray;
  ErrorWin.Env.Input        := (LightRed     shl 4) + Red;
  WarningWin.Border         := (Yellow       shl 4) + LightGray;
  WarningWin.Title          := (LightGray    shl 4) + LightBlue;
  WarningWin.Msg            := (Yellow       shl 4) + Black;
  WarningWin.KeyActif       := (White        shl 4) + LightRed;
  WarningWin.KeyUnactif     := (White        shl 4) + Black;
  WarningWin.KeyShade       := (Yellow       shl 4) + DarkGray;
  RemWin.Border             := (LightBlue    shl 4) + LightGray;
  RemWin.Title              := (LightGray    shl 4) + Black;
  RemWin.Msg                := (LightBlue    shl 4) + LightCyan;
  RemWin.KeyActif           := (White        shl 4) + LightRed;
  RemWin.KeyUnactif         := (White        shl 4) + Black;
  RemWin.KeyShade           := (LightBlue    shl 4) + Black;
  OpenWin.Window.Title        := (LightBlue    shl 4) + LightGray;
  OpenWin.Window.Border       := (LightGray    shl 4) + Blue;
  OpenWin.Window.Icon         := (Black        shl 4) + White;
  OpenWin.Window.Sel          := (LightGray    shl 4) + Blue;
  OpenWin.Window.High         := (LightGray    shl 4) + Cyan;
  OpenWin.Window.Key          := (White        shl 4) + Black;
  OpenWin.Window.kShade       := (LightGray    shl 4) + Black;
  OpenWin.Window.kSel         := (White        shl 4) + LightRed;
  OpenWin.Env.Input         := (White        shl 4) + Black;
  OpenWin.Env.Dir           := (White        shl 4) + Black;
  MalteDos.Window.Title       := (White        shl 4) + Black;
  MalteDos.Window.Border      := (Black        shl 4) + White;
  MalteDos.Window.Icon        := (Black        shl 4) + White;
  MalteDos.Window.Sel         := (Black        shl 4) + LightBlue;
  MalteDos.Window.High        := (Black        shl 4) + LightCyan;
  MalteDos.Window.Key         := (White        shl 4) + Black;
  MalteDos.Window.kShade      := (Black        shl 4) + Black;
  MalteDos.Window.kSel        := (White        shl 4) + LightRed;
  MalteDos.Env.Default      := (Black        shl 4) + LightGray;
  MalteDos.Env.Prompt       := (Black        shl 4) + LightGreen;
  Editor.Window.Title  := (LightBlue    shl 4) + White;
  Editor.Window.Border := (White        shl 4) + Black;
  Editor.Window.Icon   := (Black        shl 4) + White;
  Editor.Window.Sel    := (White        shl 4) + Blue;
  Editor.Window.High   := (White        shl 4) + Cyan;
  Editor.Window.Key    := (White        shl 4) + Black;
  Editor.Window.kShade := (White        shl 4) + Black;
  Editor.Window.kSel   := (White        shl 4) + LightRed;
  Editor.Env.BarInfo        := (LightGray    shl 4) + Black;
  Editor.Env.Default        := (White        shl 4) + Magenta;
  Editor.Env.Typewriter     := (White        shl 4) + Black;
  Editor.Env.Rem            := (Yellow       shl 4) + Black;
  Editor.Env.ResWord        := (White        shl 4) + Black;
  Editor.Env.Chars          := (White        shl 4) + Red;
  Editor.Env.Symbol         := (White        shl 4) + Cyan;
  Editor.Env.AsmInst        := (White        shl 4) + Green;
  Editor.Env.Number         := (White        shl 4) + Black;
  Editor.Env.Z              := (LightMagenta shl 4) + Black;
  Editor.Env.Normal         := (White        shl 4) + Black;
  Editor.Env.Pos            := (LightGray    shl 4) + Black;
  Editor.Env.Insert         := (LightGray    shl 4) + Black;
  Editor.Env.Modified       := (LightGray    shl 4) + LightRed;
  Editor.OutZone            := (LightGray    shl 4) + Black;
  PersonalJournal.Window.Title  := (LightMagenta shl 4) + Black;
  PersonalJournal.Window.Border := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Window.Icon   := (Black        shl 4) + White;
  PersonalJournal.Window.Sel    := (LightMagenta shl 4) + White;
  PersonalJournal.Window.High   := (Blue         shl 4) + LightCyan;
  PersonalJournal.Window.Key    := (White        shl 4) + Black;
  PersonalJournal.Window.kShade := (Blue         shl 4) + Black;
  PersonalJournal.Window.kSel   := (White        shl 4) + LightRed;
  PersonalJournal.Env.BarInfo := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Default := (Magenta      shl 4) + LightMagenta;
  PersonalJournal.Env.Pos     := (LightMagenta shl 4) + Magenta;
  PersonalJournal.Env.Insert  := (LightMagenta shl 4) + Magenta;
  ViewAscii.Window.Border := (White        shl 4) + Black;
  ViewAscii.Window.Title  := (LightBlue    shl 4) + Black;
  ViewAscii.Window.Sel    := (Blue         shl 4) + White;
  ViewAscii.Window.High   := (LightBlue    shl 4) + LightCyan;
  ViewAscii.Window.kSel   := (White        shl 4) + LightRed;
  ViewAscii.Window.Key    := (White        shl 4) + Black;
  ViewAscii.Window.kShade := (LightBlue    shl 4) + Black;
  ViewAscii.Env.BarInfo          := (LightCyan    shl 4) + Black;
  ViewAscii.Env.Default          := (Blue         shl 4) + LightMagenta;
  ViewAscii.Env.Rem              := (Blue         shl 4) + Yellow;
  ViewAscii.Env.ResWord          := (Blue         shl 4) + White;
  ViewAscii.Env.Chars            := (Blue         shl 4) + LightRed;
  ViewAscii.Env.Symbol           := (Blue         shl 4) + LightCyan;
  ViewAscii.Env.AsmInst          := (Blue         shl 4) + LightGreen;
  ViewAscii.Env.Number           := (Blue         shl 4) + LightBlue;
  ViewAscii.Env.Z                := (LightMagenta shl 4) + Blue;
  ViewAscii.Env.Normal           := (Blue         shl 4) + White;
  ViewAscii.Env.Pos              := (LightCyan    shl 4) + Blue;
  ViewAscii.Env.Insert           := (LightCyan    shl 4) + LightMagenta;
  ViewAscii.Env.Modified         := (LightCyan    shl 4) + LightRed;
  HexView.Window.Title  := (LightBlue    shl 4) + LightGray;
  HexView.Window.Border := (LightGray    shl 4) + Black;
  HexView.Window.Icon   := (Black        shl 4) + White;
  HexView.Window.Sel    := (Blue         shl 4) + White;
  HexView.Window.High   := (LightGray    shl 4) + Cyan;
  HexView.Window.Key    := (White        shl 4) + Black;
  HexView.Window.kShade := (LightGray    shl 4) + Black;
  HexView.Window.kSel   := (White        shl 4) + LightRed;
  HexView.Env.Input   := (Blue         shl 4) + White;
  EditButt.Title  := (LightBlue    shl 4) + LightGray;
  EditButt.Border := (LightGray    shl 4) + Black;
  EditButt.Icon   := (Black        shl 4) + White;
  EditButt.Sel    := (Blue         shl 4) + White;
  EditButt.High   := (LightGray    shl 4) + Cyan;
  EditButt.Key    := (White        shl 4) + Black;
  EditButt.kShade := (LightGray    shl 4) + Black;
  EditButt.kSel   := (White        shl 4) + LightRed;
  Dial.Window.Title  := (LightBlue    shl 4) + White;
  Dial.Window.Border := (White        shl 4) + Blue;
  Dial.Window.Icon   := (Black        shl 4) + White;
  Dial.Window.Sel    := (White        shl 4) + Blue;
  Dial.Window.High   := (White        shl 4) + Cyan;
  Dial.Window.Key    := (LightGray    shl 4) + Black;
  Dial.Window.kShade := (White        shl 4) + Black;
  Dial.Window.kSel   := (White        shl 4) + LightRed;
  Dial.Env.BarUnSelect := (White        shl 4) + Blue;
  Dial.Env.BarSelect   := (White        shl 4) + DarkGray;
  Draw.Window.Title  := (LightBlue    shl 4) + White;
  Draw.Window.Border := (LightGray    shl 4) + White;
  Draw.Window.Icon   := (Black        shl 4) + White;
  Draw.Window.Sel    := (Blue         shl 4) + White;
  Draw.Window.High   := (LightBlue    shl 4) + LightCyan;
  Draw.Window.Key    := (White        shl 4) + Black;
  Draw.Window.kShade := (LightBlue    shl 4) + Black;
  Draw.Window.kSel   := (White        shl 4) + LightRed;
  Help.Window.Title  := (White        shl 4) + Black;
  Help.Window.Border := (Black        shl 4) + White;
  Help.Window.Icon   := (Black        shl 4) + White;
  Help.Window.Sel    := (Black        shl 4) + LightBlue;
  Help.Window.High   := (Black        shl 4) + LightCyan;
  Help.Window.Key    := (White        shl 4) + Black;
  Help.Window.kShade := (Black        shl 4) + Black;
  Help.Window.kSel   := (White        shl 4) + LightRed;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.Window.Title := (LightBlue    shl 4) + White;
  FileManager.Window.Border:= (LightGray    shl 4) + Black;
  FileManager.Window.Icon  := (Black        shl 4) + White;
  FileManager.Window.Sel   := (LightBlue    shl 4) + White;
  FileManager.Window.High  := (LightGray    shl 4) + Cyan;
  FileManager.Window.Key   := (White        shl 4) + Black;
  FileManager.Window.kShade:= (LightGray    shl 4) + Black;
  FileManager.Window.kSel  := (White        shl 4) + LightRed;
  FileManager.SubTitle   := (LightGray    shl 4) + Black;
 End;
End;

BEGIN
 WriteLn('MakeColor - Cr‚ateur d''environnement des couleurs');
 InitColorsDefault;
 SetFile('ISABEL.COL',0,SizeOf(Colors),Colors);
END.